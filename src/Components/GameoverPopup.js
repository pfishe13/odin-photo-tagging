import React from 'react';

const GameoverPopup = () => {
  return (
    <div className="instructions-popup">
      <h2>GAME OVER</h2>
      <button>Start</button>
    </div>
  );
};

export default GameoverPopup;
